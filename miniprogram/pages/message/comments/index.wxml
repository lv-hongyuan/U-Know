<view class="page comments-page">
  <scroll-view
    class="comments-scroll"
    scroll-y
    enhanced
    show-scrollbar="{{false}}"
    refresher-enabled
    refresher-triggered="{{refreshing}}"
    bindrefresherrefresh="onRefresh"
    bindscrolltolower="onLoadMore"
    lower-threshold="120"
  >
    <list-skeleton wx:if="{{loading && list.length === 0}}" type="notify" rows="{{8}}" />

    <view
      class="comments-item"
      wx:for="{{list}}"
      wx:key="id"
      data-postid="{{item.postId}}"
      data-missing="{{item.postMissing ? 1 : 0}}"
      data-commentid="{{item.commentId}}"
      data-parentid="{{item.parentId}}"
      data-commentmissing="{{item.commentMissing ? 1 : 0}}"
      hover-class="comments-item--pressed"
      hover-stay-time="80"
      bindtap="onTapItem"
    >
      <image
        class="comments-avatar"
        src="{{item.fromAvatarUrl || defaultAvatar}}"
        mode="aspectFill"
        data-openid="{{item.fromOpenid}}"
        catchtap="onTapAvatar"
      />
      <view class="comments-main">
        <view class="comments-head">
          <text class="comments-name">{{item.fromNickName}}</text>
          <text class="comments-action">{{item.actionText}}</text>
        </view>
        <view class="comments-origin" wx:if="{{item.originalText}}">
          <text class="comments-origin-label">{{t.message.originalComment}}</text>
          <text class="comments-origin-text">{{item.originalText}}</text>
        </view>
        <view class="comments-title" wx:if="{{item.previewText}}">{{item.previewText}}</view>
        <view class="comments-time">{{item.timeText}}</view>
      </view>
      <view
        class="comments-cover-wrap"
        wx:if="{{item.postCover}}"
      >
        <image
          class="comments-cover"
          src="{{item.postCover}}"
          mode="aspectFill"
        />
      </view>
    </view>

    <view class="comments-empty" wx:if="{{!loading && list.length === 0}}">
      <image class="comments-empty-icon" src="/images/empty.svg" mode="aspectFit" />
      <text class="comments-empty-text">{{t.common.noData}}</text>
    </view>
    <view class="comments-loading" wx:if="{{loading && list.length > 0}}">{{t.common.loading}}</view>
    <view
      class="comments-end"
      wx:if="{{!loading && list.length > 0 && !hasMore}}"
    >{{t.message.noMore}}</view>
  </scroll-view>
</view>
