---
description: All UI work must use the ui-ux-pro-max skill
alwaysApply: true
---

# UI / UX Pro Max（强制）

凡涉及 **界面外观、布局、组件视觉、交互反馈、动效、配色、字体、间距** 的改动，必须先阅读并遵循 Cursor skill：

`~/.cursor/skills/ui-ux-pro-max/SKILL.md`

（也可用 skills 列表中的 `ui-ux-pro-max`）

## 何时必须启用

- 新建 / 改造页面或弹层、列表、Tab、表单、空状态
- 调整颜色、圆角、阴影、图标、排版、间距
- 用户反馈「丑 / 不清晰 / 不好点 / 不专业」类 UI 问题
- 做视觉 polish、设计一致性检查

## 何时可跳过

- 纯云函数 / 数据表 / 鉴权逻辑
- 与界面无关的脚本、配置、部署

## 落地要求

1. **先读 skill 再改 UI**，按其中优先级（无障碍 → 触控 → 布局 → 字体配色等）做决策。
2. **贴合现有产品视觉**：本小程序主色为红橙渐变系（如 `#FF6B1A` / `#FF3B30`→`#FF9500` / `#FFB347` / `#3D1408`），背景 `#FFF6F0`；不要另起一套无关风格。
3. **图标用 SVG**，禁止 emoji 当图标；可点击区域需有文案或明确语义，触控热区够大。
4. 改 UI 时同步遵守 `miniprogram-i18n`：可见文案走 i18n，不硬编码。
